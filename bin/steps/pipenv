# Pipenv support (Generate requriements.txt with pipenv).
if [[ -f Pipfile ]]; then
    if [[ ! -f requirements.txt ]]; then
        puts-step "Installing requirements with pipenv..."

        /app/.heroku/python/bin/pip install pipenv install --system &> /dev/null
        export SKIP_PIP_INSTALL=1
    fi
fi