#!/usr/bin/env bash

if [ $# -eq 0 ] || ([ "$1" != "online" ] && [ "$1" != "offline" ]); then
  echo "Usage:"
  echo "  ./package [online|offline]"
  exit 1
fi

BASE="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source $BASE/create_python_buildpack.sh $1