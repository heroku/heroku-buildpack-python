[project]
name = "ci-poetry"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "typing-extensions",
]

[dependency-groups]
dev = [
    "pytest",
]


[tool.poetry]
package-mode = false
