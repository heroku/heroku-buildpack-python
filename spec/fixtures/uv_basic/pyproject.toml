[project]
name = "uv-basic"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = [
    "typing-extensions",
]

[dependency-groups]
# This group shouldn't be installed due to us passing `--no-default-groups`.
dev = [
    "pytest",
]
