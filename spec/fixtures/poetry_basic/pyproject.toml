[project]
name = "poetry-basic"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "typing-extensions",
]

[dependency-groups]
# This group shouldn't be installed due to us passing `--only main`.
dev = [
    "pytest",
]

[tool.poetry]
package-mode = false
